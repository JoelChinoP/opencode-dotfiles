# nginx-opencode-map.conf - header Connection correcto para WebSocket.
# Este archivo debe cargarse en contexto http, no dentro de server {}.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
